#include <stdio.h>

int main(void)
{
// *-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*
// Complete el tamaño para una cadena con longitud suficiente para
// contener nombres de archivos del sistema.

    char nombre_archivo[FI];

// Respuesta sugerida {{{
//     Si se completa normalmente, las opciones incluirán FILE y
//     FILENAME_MAX. Si se completa con <C-x><C-d> la única opción será
//     FILENAME_MAX que es lo que queremos. En cualquier caso <C-n> y <C-p>
//     siguen sirviendo para recorrer opciones
// }}}
// *v*v*v*v*v*v*v*v*v*v*v*v*v*v*v*v*v*v*v*v*
}
// vim: sts=4 ts=4 sw=4 ai sta nu fdm=marker tw=75
