#include <stdio.h>

int main(void)
{
// *-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*
// Ejercicio:
// Complete el tamaño para una cadena con longitud suficiente para
// contener nombres de archivos del sistema.
//
// Respuesta sugerida {{{
//     Si se completa normalmente, las opciones incluirán FILE y
//     FILENAME_MAX. Si se completa con <Ctrl-x><Ctrl-d> la única
//     opción será FILENAME_MAX que es lo que queremos.
//     En cualquier caso <Ctrl-n> y <Ctrl-p> siguen sirviendo
//     para recorrer opciones
// }}}

    char nombre_archivo[FI];

// *v*v*v*v*v*v*v*v*v*v*v*v*v*v*v*v*v*v*v*v*
}
// vim: sts=4 ts=4 sw=4 ai sta nu fdm=marker
