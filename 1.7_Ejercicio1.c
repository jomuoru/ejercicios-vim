#include <stdio.h>

int main(void)
{
    // *-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*
    // Extrae la cadena a imprimir a una variable, es decir, pasa de la
    // forma:
    //
    //  puts("cadena")
    //
    // a la forma:
    //
    //  char var[] = "cadena";
    //  puts(var);

    puts("Amet non libero autem doloremque?");
    // Respuesta sugerida (Abrir o cerrar con) {{{
    //     Dentro de los paréntesis de puts() usa ci)var<C-[>
    //     Inserta una línea por encima con o y escribe
    //         char var[] = ;<C-[>
    //     Pega la cadena con P
    // }}}
    // *v*v*v*v*v*v*v*v*v*v*v*v*v*v*v*v*v*v*v*v*
}

// Volver a la lección: 1.7_Operadores.txt:23 (Regresar con gF)

// vim: sts=4 ts=4 sw=4 ai sta nu fdm=marker
